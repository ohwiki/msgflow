{
  "name": "msgflow-worker-v2",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --env production",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "check": "tsc --noEmit && vitest run",
    "types": "wrangler types"
  },
  "dependencies": {
    "mustache": "4.2.0",
    "turndown": "7.2.0"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "0.9.0",
    "@cloudflare/workers-types": "4.20250514.0",
    "@types/mustache": "4.2.5",
    "typescript": "5.8.3",
    "vitest": "3.1.3",
    "wrangler": "4.88.0"
  }
}
